version: '3'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: pia
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: todo_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: